<section id="top">
  <div class="container">
    <div class ="top-message">
    　ようこそながのCakeへ！<br>
      このサイトは、ケーキ販売のECサイトになります。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。<br>
    </div>
  </div>
  <div class="container">
    <div class="row">
      <h3 class="ml-3"><i class="fas fa-arrow-circle-up"></i> 新着商品</h3>
      <div class="card-deck">
        <% @items.each do |item| %>
          <div class="col-md-3">
            <div class="card m-3 w-100 h-auto">
              <%= link_to item_path(item) do %>
                <%= attachment_image_tag item, :image_id, :fill, 50, 50, fallback: "mizuho4141396IMG_7893_TP_V.jpg", class: "card-img-top", alt: "商品イメージ", width: "100%"%>
              <% end %>
              <div class="card-body">
                <p class="card-title item-name"><%= link_to item.name, item_path(item) %></p>
                <p class="card-text  text-dark">￥<%= item.price %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <%= paginate @items %>
  <h5><%= link_to "すべての商品を見る >", items_path, class:"text-white font-weight-bold" %></h5>
</section>