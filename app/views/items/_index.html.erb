<div class="row">
  <div class="card-deck">
    <% items.each do |item| %>
      <div class="col-md-3">
        <div class="card h-300">
          <%= link_to item_path(item) do %>
            <%= attachment_image_tag item, :image_id, :fill, 50, 50, fallback: "mizuho4141396IMG_7893_TP_V.jpg", class: "card-img-top", alt: "商品イメージ", size: "50x50" %>
          <% end %>
          <div class="card-body">
            <p class="card-title"><%= link_to item.name, item_path(item) %></p>
            <p class="card-text">￥<%= item.price %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<%= paginate items %>