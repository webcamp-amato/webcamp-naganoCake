<section id="top">
  <div class="container pb-3">
    <%= image_tag "logo-light.png", width: '50%', height: '50%' %>
    <div class ="top-message">
    　ようこそながのCakeへ！<br>
      このサイトは、ケーキ販売のECサイトになります。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。<br>
    </div>
  </div>
</section>
<div class="container mt-3 mb-0">
  <div class="row">
    <div class="col-md-2">
      <table class="table table-borderless table-hover">
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td>
                <%= genre.name %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-10">
      <h3 class="ml-4 text-left"><i class="fas fa-arrow-circle-up"></i>新着商品</h3>
      <div class="card-deck top-cards">
        <% @items.each do |item| %>
          <div class="card col-md-3 my-3 bg-light top-card">
            <%= link_to item_path(item) do %>
              <%= attachment_image_tag item, :image, format: 'jpeg', fallback: "mizuho4141396IMG_7893_TP_V.jpg", class: "card-img-top", alt: "商品イメージ", size: "200x120"%>
            <% end %>
            <div class="card-body">
              <p class="card-title item-name"><%= link_to item.name, item_path(item) %></p>
              <p class="card-text text-dark">￥<%= item.price %></p>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="col-12 text-right">
      <h5><%= link_to "すべての商品を見る >", items_path, class:"font-weight-bold" %></h5>
    </div>
  </div>
</div>